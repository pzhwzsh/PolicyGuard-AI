# Compliance Review Skill

Use this skill only for internal evidence review. It may read the supplied context and call the
deterministic PolicyGuard publish preflight tool. It cannot publish, send messages, access the
network, execute code, or approve its own result. A human must review the final output.
