global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: policyguard-api
    metrics_path: /metrics
    authorization:
      credentials_file: /run/secrets/metrics_api_key
    static_configs:
      - targets: ["api:8000"]
